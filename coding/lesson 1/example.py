name = input("your name : ")
individual_names = name.split(" ") # makes sure that a name for example unais is 1 word not 5 characters
print (f"you have {len(individual_names)} names. ") # calculates length of the name(in this instance how many names you have)